#include <bits/stdc++.h>

using namespace std;

int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);

    // 1. 读取多个元素，可能有至少一个空格，或者有换行
    // 123 456 monkey
    /**
     * 123    456
     * monkey
    */
    // int a, b;
    // string x;
    // cin >> a >> b >> x;
    // cout << a << " " << b << " " << x << endl;

    // 2. 读取整行，包括空格
    // string s;
    // getline(cin, s);
    // cout << s << endl;

    // 3. 未知数量
    // string x;
    // while (cin >> x) {
        // code
    // }

    // 4. 读写文件
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    
}
